[project]
name = "mirages"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rosa, Ada", email = "arosa@amd.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
mirages = "mirages:main"

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"
